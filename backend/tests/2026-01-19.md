### 项目背景

这是 **ETSY 订单自动化系统** 的前端界面开发项目，基于 HTML + Tailwind CSS 构建静态页面原型，之后导入 Pixso 调整，再同步为 Vue 组件。

工作区路径：`d:\ETSY_Order_Automation`  
测试页面路径：`d:\ETSY_Order_Automation\backend\tests\`

---

### 当前已完成的 6 个页面

|文件名|状态|
|---|---|
|`生产订单管理系统_待确认订单OK.html`|✅ 设计基准稿，已完成|
|`仪表盘总览.html`|✅ 已完成|
|`生产中订单.html`|✅ 刚完成优化|
|`已完成订单.html`|✅ 刚完成优化|
|`物流追踪.html`|✅ 已完成|
|`邮件模板.html`|❌ 尚未创建，是下一步工作|

---

### 页面规范（必须严格遵守）

1. **视觉基准**：所有页面以 `生产订单管理系统_待确认订单OK.html` 为设计规范
2. **容器尺寸**：`h-[900px] w-[1480px]`，`bg-white shadow-2xl rounded-2xl`
3. **字体**：Inter，通过 Google Fonts 引入
4. **背景**：`body` 用 `bg-slate-200`
5. **SVG 图标**：全部内联，颜色用固定十六进制，禁止 `currentColor`

### 固定左侧导航 6 项（顺序固定）
# 2026-01-19
```
仪表盘总览 / 待确认订单（badge:32）/ 生产中订单（badge:18，当前页蓝色高亮） 已完成订单（badge:64，当前页绿色高亮）/ 物流追踪 / 邮件模板
```
### 刚刚完成的改动（本次对话最终状态）

**生产中订单.html（颜色控制）：**

- 第二逾期行 `#PO-2603-002`：背景去橙（`bg-orange-50` → 白色hover），ID 颜色去橙（`text-orange-700` → `text-slate-700`），进度条底色（`bg-orange-100` → `bg-slate-100`）
- 详情区生产进度 badge：去掉大橙底（`bg-orange-500 text-white` → `bg-slate-100 text-slate-600`），只保留「须跟进」为橙色文字
- **仅保留橙色的位置**：逾期交货日期文字 + ⚠、状态列「须跟进」badge、详情区「⚠ 已逾期·须跟进」badge、进度条填充颜色、底部警告文字

**已完成订单.html（字段修复 + 追评邮件）：**

- 表头 11 列：`订单ID / 客户名称 / 产品 / 数量 / 完成日期 / 快递公司 / 运单号 / 物流状态 / 已交货天数 / 生产文档 / 邮件追评`
- 已修复 4 行脏数据（`#PO-2602-004/005 / #PO-2601-058/057`）：删除残留「定制内容」`<td>`，已签收行补「已交货天数」（4天/5天/6天），「邮件追评」列统一改为 `<select>` 下拉（准备发送/无须发送）
- 详情区物流轨迹下方新增「追评邮件」卡片：状态 badge（准备发送）、英文邮件正文展示、「编辑邮件」+「发送追评邮件」按钮

---

### 邮件功能模块职责分工（已确认规范）

|位置|职责|
|---|---|
|待确认订单页 — 客户需求区|记录单笔订单的客户原始需求与往来疑问，长期保留，沉淀为客服知识库|
|待确认订单页 — 邮件生成|快速生成第一封确认邮件并发送|
|生产中订单页 — 发货通知按钮|该笔订单发货节点的通知邮件|
|已完成订单页 — 追评邮件正文区|该笔订单的追评邮件内容与发送状态|
|导航「邮件模板」页|**运营话术知识库**：首封邮件范例、自然语言风格、常见问题回复等多板块，非针对单订单|

---

### 下一步要做的工作

**目标：创建 `邮件模板.html`**，信息架构如下：

- **板块 1**：首封确认邮件模板（下单后第一封）
- **板块 2**：自然语言风格话术（如何显得友好自然）
- **板块 3**：延迟/异常说明模板（工厂延迟、物流问题）
- **板块 4**：追评邮件模板（签收后催评）
- **板块 5**：常见问题回复话术（客户问修改/退款等）

每个板块展示：分类标签 + 模板列表 + 右侧预览区，整体风格与其他 5 个页面保持一致。

---

### 技术注意事项

- `search_replace` 必须传 `replacements` 数组格式
- 一次只替换唯一片段，避免大块替换失败
- Windows PowerShell 用 `;` 分隔命令，不用 `&&`
- PDF 文件路径：`backend/output/POD_3891559803_20260217_222758.pdf`

---

**请在新对话中继续创建 `邮件模板.html`，按上述信息架构和设计规范完整输出。**